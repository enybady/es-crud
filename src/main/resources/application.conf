my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 60s
  }
  server {
    port = "8080"
  }

  es {
    host = "0.0.0.0"
    port = "0.0.0.0"
  }
}